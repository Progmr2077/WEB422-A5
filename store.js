import { atom } from 'jotai';

// Jotai atom to store the list of favourite artworks
export const favouritesAtom = atom([]);

// Jotai atom to store the search history as an array
export const searchHistoryAtom = atom([]);